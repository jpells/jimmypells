{
  "devDependencies": {
    "@biomejs/biome": "2.0.0-beta.1",
    "tailwindcss": "^3.1.8"
  },
  "scripts": {
    "check": "biome ci",
    "format": "biome check --fix",
    "build": "tailwindcss -i ./src/styles.css -o ./assets/css/styles.css --minify"
  }
}
